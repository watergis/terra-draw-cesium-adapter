---
'@watergis/terra-draw-cesium-adapter': minor
---

First release of the Terra Draw adapter for CesiumJS. Renders points, markers, linestrings and polygons as ground-clamped Cesium entities, and takes the Cesium namespace as an injected `lib` so the package carries no runtime dependency on Cesium.
